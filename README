This is a merge sort algorithm implementation using JavaScript. Just clone the repository, open the index.html in you web browser and check the console. You could see a random array of integers to become ordered.